# Used by both docker-compose.yml (Postgres container) and Prisma.
# These are local-only placeholder defaults — never use them for a real database.
POSTGRES_USER="womp"
POSTGRES_PASSWORD="womp_dev_only"
POSTGRES_DB="womp"

DATABASE_URL="postgresql://womp:womp_dev_only@localhost:5432/womp"
